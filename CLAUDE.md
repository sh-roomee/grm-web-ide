# grm-web-ide 작업 규칙

## 이 프로젝트가 무엇인가

터미널에서 AI로 개발할 때, git log나 diff를 보기 위해 어쩔 수 없이 IDE를 켜는
번거로움을 없애기 위한 브라우저 git 뷰어. `gitshow`를 치면 현재 디렉토리의
변경사항이 브라우저에 뜬다.

**IDE를 대체하는 것이 목표가 아니다. IDE를 켜는 이유를 없애는 것이 목표다.**

## 범위 규칙 (중요)

기준선은 **"편집이 없는 읽기 전용 IDE"** 다. IDE를 켜는 이유를 없애려면 기존
코드를 읽는 일은 피할 수 없다. 그래서 읽기는 넓게 허용하고, 쓰기는 좁게 막는다.

넣지 않는 것:

- **코드 편집 기능 (에디터)** — 이것이 유일하고 절대적인 선이다
- 터미널 임베드
- 원격·이력 조작: push / pull / fetch / rebase / reset --hard

허용하는 쓰기 동작은 **stage / unstage / commit** 까지다.

읽기 쪽에서 허용하는 것(2026-07-25 결정, 그 전에는 금지였다):

- 변경되지 않은 파일도 열어서 본다 (읽기 전용 뷰어)
- 파일 검색(⌘P), 전체 텍스트 검색(⌘⇧F), 통합 검색(Shift 두 번)
- 다만 **git이 아는 파일만** 다룬다: 목록은 `git ls-files`, 검색은 `git grep`.
  파일시스템을 직접 훑지 않으므로 `.gitignore`가 자동으로 지켜지고, 이 도구가
  "git이 보는 세계"에 머문다.

### 왜 이렇게 바꿨나

처음 규칙은 "파일 트리 브라우징 금지 — 변경된 파일만"이었다. 실제로 써 보니
diff만으로는 판단이 안 되는 순간이 계속 나왔다. 바뀐 줄의 앞뒤가 아니라 그 함수를
쓰는 다른 파일을 봐야 하고, 그러려고 IDE를 켜면 이 도구의 존재 이유가 사라진다.

대신 선을 다른 곳에 그었다. **디렉토리를 펼쳐 돌아다니는 UI는 만들지 않는다.**
파일에 도달하는 길은 검색뿐이다. 트리가 없으면 "파일 관리자"가 되지 않고, 편집이
없으면 "에디터"가 되지 않는다.

## 문서화 규칙

코드를 바꿀 때마다 아래 문서를 같은 변경 안에서 갱신한다. 문서가 코드와
어긋난 상태로 커밋하지 않는다.

| 문서 | 갱신해야 하는 때 |
| --- | --- |
| `docs/ARCHITECTURE.md` | 모듈 추가/삭제, 데이터 흐름 변경, 설계 결정 |
| `docs/API.md` | HTTP 엔드포인트나 응답 스키마 변경 |
| `docs/ROADMAP.md` | 기능 완료, 우선순위 변경, 새 항목 추가 |
| `README.md` | 설치/실행 방법, CLI 옵션 변경 |
| `CLAUDE.md` | 범위 규칙이나 작업 방식 변경 |

설계 판단을 바꿀 때는 `docs/ARCHITECTURE.md`의 "설계 결정" 절에 **왜 그렇게
했는지**를 남긴다. 결론만 적으면 나중에 되돌릴 근거가 사라진다.

## 검증 방식

- 파서 등 순수 로직은 `test/`에 노드 기본 테스트로 남긴다: `npm test`
- 서버/UI 변경은 임시 git 저장소를 만들어 실제로 띄워 확인한다.
  API만 200이 오는 것으로는 부족하고, 화면에 제대로 그려지는지까지 본다.

## 스택

- 서버: Node 18+ / Express. git은 항상 `execFile`로 인자 배열 전달 (셸 금지)
- 프론트: Vue 3 (SFC, `<script setup>`) / Vite
- 상태 관리 라이브러리 없음. `composables/`로 충분하다.

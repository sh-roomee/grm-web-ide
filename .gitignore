node_modules/
web/dist/
.idea/
.claude/settings.local.json
*.log
.DS_Store

# 화면 기록 원본. README 의 영상은 GitHub 첨부로 호스팅하므로 저장소에 둘 이유가
# 없다 — 4K 화면 기록 한 개가 31MB 라서 한 번 커밋하면 모든 클론이 영구히 받는다.
# 문서에 박아 넣을 짧은 .webp/.gif 는 이미지라 여기에 걸리지 않는다.
*.mov
*.m4v
